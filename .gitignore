# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.*.local
.vercel

# Secrets — NEVER commit (this is a PUBLIC repo). Play/GCP service-account keys,
# signing keystores, raw key material. The real files live in the iCloud
# "LevelUp Core/keystores" vault, not here.
*service_account*.json
*-sa.json
**/levelups-tagmango-app-*.json
*.jks
*.keystore
*.keyfile
*.p12
*.p8
AuthKey_*.p8

# Supabase CLI scratch state (project ref, pooler URL, versions, etc.)
supabase/.temp/

# Deno lockfile — only generated locally when type-checking edge functions
# (supabase deploy bundles server-side and does not consume it).
deno.lock
design/qa/
design/vision/shots/
