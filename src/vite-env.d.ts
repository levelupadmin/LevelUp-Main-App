/// <reference types="vite/client" />
// build trigger 1775630958

// Injected by vite `define` (see buildStamp() in vite.config.ts). The short
// commit SHA this bundle was built from, and when — surfaced in the preview
// so a stale alias is visible on screen instead of silently serving old code.
declare const __BUILD_SHA__: string;
declare const __BUILD_TIME__: string;
