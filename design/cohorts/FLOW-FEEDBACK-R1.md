# Flow-vision — Rahul's Round-1 feedback (2026-07-16)

Verbatim-intent log of every point. v2 must address each; items marked KEEP are approved as-is.

## Identity & entry
1. **Auth clarity required.** Spell out exactly how Tally application → app works: applicant gives phone+email in the form → they become an app user AUTOMATICALLY (no signup step ever), staged/categorized as an applicant. Sign-in = OTP on phone OR email. Zero added friction. v2 needs an "identity spine" explainer (form → auto-provisioned account → OTP login → staged state).
2. **The install question.** Why would an abandoner install the app? Solve the motivation for installing (and fully support the web-app path for those who don't). Flesh out the install/web nudge strategy.
3. KEEP: open-loop first screen. Add Duolingo-style notification/reminder strategy for finishing applications (conversion-oriented nudge design).

## Copy & personalization rules
4. **NEVER surface the 100-word essay text in any UI** (people rage-type garbage). Kills "mirror their own why" as-designed. Personalize from STRUCTURED fields instead (name, cohort, craft/quiz answers, city…), never free text.
5. KEEP: "Your application is saved. Two taps to finish. The draft is exactly where you left it."
6. KEEP: "The review batch for this cohort closes at [time] — lock your application."
9a. Copy principle: "The one prerequisite for any cohort is the passion to learn." "Unfinished" ok, "untouched" ok — soften any judgmental framing in wait screens.

## Interview stage
7. Interviewers are Business Development Associates (sales), NOT mentors. Never "counselor". Find a better title ("interviewer" or better — propose options). Use their real names, NO bios; instead a selectivity metric per interviewer (e.g. "accepts 24% of applicants he interviews") to create perform-well FOMO. (8: open to smarter alternatives.)
9b. **Interview modality**: Google Meet OR phone call — the STUDENT CHOOSES in Calendly. UI must reflect the chosen modality (never assume Zoom).
9c. **Reschedule**: one-reschedule guardrail stays; never charge for more; NEVER use the word "free".

## Decision & acceptance
9d. KEEP decision screen ("Your decision is ready → Open your decision → Claim my seat").
9e. **Acceptance card must be Buildspace-grade shareable** (LinkedIn moment). Personalize WITHOUT the essay — find other parameters.
9f. **Open-your-decision = a brilliant full-viewport animation**, AND the moment must yield an auto-generated SHAREABLE VIDEO (not phone screen-recording): a beautiful animation with their name, shareable to stories. Design the mechanism (pre-rendered/generated video artifact) + the interaction.
9g. **Flesh out**: the "Claim my seat" flow AND "Read the enrollment details" flow (what happens on each).
9h. **Shared-admission link**: wireframe what a RECIPIENT sees when an admission is shared as a link (public admission page).

## Confirmation & pre-cohort
9i. **REMOVE seat numbers** (low numbers signal empty cohort). Replace with a **locked future view**: they can SEE what's inside the cohort (locked previews) → excitement to confirm unlocks it.

## Cohort room — NOT SOLD, redesign
9j. Current room flow feels "outdated, clunky, boring." Redesign with more thought, HIGHER fidelity, world-class modern attention to detail.
9k. No filmmaking terms as cringe — BUT per-SKU terminology is wanted where it genuinely fits: each cohort/SKU can have its own configurable vocabulary (an AI-cohort mentor sees different terms than a film cohort). Show this.
9l. Build flow variants (or categorized views) for: **the AI cohort** (unlaunched — reference material: the /ai-cohort page in the levelupnewsite repo + Rahul may share more) and **LevelUp Creator Academy** (live). Personalize per SKU where it matters; otherwise same flow, categorized per SKU.
9m. **MISSING: in-room community.** The cohort room needs its own community — members talking to each other, separated by categories/topics/niches. Flesh out that flow. (Distinct from the global generic commons; this is room-internal.)

## Process
- The v1 PDF deck (in progress) documents v1; a v2 deck follows once revised flows are approved.
