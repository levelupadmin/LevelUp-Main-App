project_id = "twqagwleffjggoemzfqs"

[functions]
  [functions.auth-email-hook]
    verify_jwt = false
  [functions.check-user-exists]
    verify_jwt = false
  [functions.create-razorpay-order]
    verify_jwt = false
  [functions.guest-create-order]
    verify_jwt = false
  [functions.process-email-queue]
    verify_jwt = true
  [functions.razorpay-webhook]
    verify_jwt = false
  [functions.send-notification]
    verify_jwt = false
  [functions.verify-event-payment]
    verify_jwt = false
  [functions.verify-razorpay-payment]
    verify_jwt = false
  [functions.get-vdocipher-otp]
    verify_jwt = false
  [functions.vdocipher-upload-credential]
    verify_jwt = false
  [functions.verify-msg91-otp]
    verify_jwt = false
  [functions.register-for-event]
    verify_jwt = true
  # NOTE: tally-application-webhook is called by Tally with no Supabase JWT.
  # It is verify_jwt=true on prod today, which likely stops Tally reaching it
  # (it self-authenticates via HMAC, so false is the correct setting once the
  # cohort Tally flow is tested end-to-end). Kept true here to match current
  # prod and avoid an unverified behaviour change in this deploy.
  [functions.tally-application-webhook]
    verify_jwt = true
  [functions.generate-invoice-pdf]
    verify_jwt = false
