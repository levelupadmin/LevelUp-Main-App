<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--
          Solid dark window background (was @null). @null fell back to the
          platform default (white in light mode), producing a white flash
          between splash dismiss and the WebView's first paint. A dark fill
          that matches the web app bg keeps the cold-start transition seamless.
        -->
        <item name="android:windowBackground">@color/splashBackground</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ splash window background + the legacy splash drawable. -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="android:windowBackground">@color/splashBackground</item>
        <item name="android:background">@drawable/splash</item>
        <!-- Hand off to the dark-backed main theme so there is no flash. -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>